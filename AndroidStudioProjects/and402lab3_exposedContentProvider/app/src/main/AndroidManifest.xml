<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="and402lab3.nhut.and402.lab.and402lab3_exposedcontentprovider">
    <permission android:name="readSecuredDataProvider"
        android:protectionLevel="dangerous"
        android:permissionGroup="readSecuredDataProviderpg"
        android:label="read_SecuredDataProvider"
        android:description="@string/readSDP"
        />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".MyContentProvider"
            android:authorities="and402lab3.nhut.and402.lab.and402lab3_exposedcontentprovider"
            android:enabled="true"
            android:exported="true" />
        <provider
            android:name=".MySecuredContentProvider"
            android:authorities="and402.lab.and402lab3_protectedcontentprovider"
            android:enabled="true"
            android:exported="true"
            android:readPermission="readSecuredDataProvider"
            />
    </application>

</manifest>