<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>

        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@style/MyAnimation.Window</item>
    </style>

    <style name="AlbumTheme" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:windowBackground">@drawable/shelf</item>-->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="slashgame" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:windowBackground">@drawable/shelf</item>-->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="bubblegame" parent="Theme.AppCompat.NoActionBar">
        <!--<item name="android:windowBackground">@drawable/shelf</item>-->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="SlideTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="SplashEndTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_end</item>

        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@style/CloseAnimation.Window</item>
    </style>

    <!-- Animations -->
    <style name="MyAnimation" />

    <!-- Animations for a non-full-screen window or activity. -->
    <style name="MyAnimation.Window" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/growfrommiddle</item>
        <item name="android:windowExitAnimation">@anim/shrinkfrommiddle</item>
    </style>
    <!-- Animations for a non-full-screen window or activity. -->
    <style name="CloseAnimation.Window" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/blur</item>
    </style>
</resources>
